{
  "framework": "nextjs",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm exec turbo build --filter=@vnbus/web",
  "outputDirectory": "apps/web/.next",
  "env": {
    "NEXT_PUBLIC_MAINTENANCE_MODE": "false"
  }
}
